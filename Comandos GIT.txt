git --version = verifica aversão do git
git init = inicia um repositorio vazio na pasta atual
git config --global user.name "Francisco Moisés Bezerra de Freitas" = Cria usuario do git
git config --global user.email "moisesfreitas20@gmail.com" = cria email do git
git status = verifica tudo que não foi salvo
git add . = Coloca os arquivos na area de transferencia
git commit -m "Menssagem" = dizer uma mensagem para identificar o commit
git log = Mostra todos os log dos commits realizados
git git remote add origin "Link do rEPOSITORIO" = vincula a pasta local do repositorio do git https://github.com/moisesfreitas32/topicosespeciais.git
git push -u origin master = fazer commit pela primeira vez 
